server:
  port: 8081

# 模块代码：与前端 App.jsx / constants/modules.js 共用同一套导航与环境切换
platform:
  module:
    code: CONFIG
    name: 配置中心

spring:
  application:
    name: middleware-platform-server-config-center
